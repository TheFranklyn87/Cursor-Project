FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

# Source is mounted as a volume in docker-compose.dev.yml
# so we don't COPY src here — it's live

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
