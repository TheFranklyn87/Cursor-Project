{
  "name": "safe-maps",
  "version": "1.0.0",
  "private": true,
  "description": "Find the safest walking route in Vancouver",
  "scripts": {
    "build-data": "node scripts/fetch-crime.js && node scripts/fetch-lighting.js",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install"
  }
}
